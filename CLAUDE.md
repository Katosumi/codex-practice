# Project Rules

This repository currently has no project-specific overrides.

Use the global Codex instructions in `~/.codex/AGENTS.md` as the SSoT for the default stack, workflow, UI guidelines, and safety rules.

Add only repository-specific overrides here.
